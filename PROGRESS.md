# PROGRESS.md — Patch Tracker

## Current state

Functional MVP, verified end-to-end on a physical device. Tracks a player roster (name, number, optional phone/email, with a detail view showing earned-patch counts and team memberships), teams (name, division, up to 8 players each), a 33-patch real-APA catalog (with a per-category icon system plus camera-captured photos for custom patches), and patch award entries — one player/session/division/date per entry, holding multiple patches each with its own independent Awarded/Owed status and fulfillment tracking, plus an optional photo of the player with the patches. Patch awards are now organized around a real `Session` entity with an app-wide "current session" that new awards default to (a Sessions tab manages starting new sessions, renaming, and switching current); a session's awards can be bulk-cleared once finished, exported to a self-contained `.zip` backup via the system file picker, and a backup can be reopened for read-only review without touching the live database. Exporting a session also finalizes it — locking its awards from further add/edit and being the only way to unlock deletion (the current session can never be deleted, finalized or not). Players and Teams can be bulk-loaded via CSV upload (upload icon on each tab; skip-and-report validation mirroring the in-app rules). Every tab has a Help (?) button that renders that screen's section of the in-repo `FEATURES.md` (bundled as an asset at build time, so the help never drifts from the doc). The Patches list also has a selection mode (top-bar checklist icon or long-press) for picking several award records and sharing them out via the system share sheet — the award photos ride along as images and a player-name + patches summary is copied to the clipboard for pasting as the caption (Facebook killed group-posting via API, so posting to the league group is a manual final tap). All work through 2026-07-04 is committed — see `NOTES.md` for the decision/bug log behind it.

**No open blockers.**

## Next action

None pending. Pick from "Suggested next steps" below by priority, or continue with new feature requests as they come in.

## Suggested next steps

- **Keep `FEATURES.md` current** — the end-user feature guide now lives in-repo at `FEATURES.md` (created 2026-07-05, current through v0.1.4). Per the CLAUDE.md convention, update it as part of every functional change. (The old external `C:\Users\james\Downloads\Patch Tracker - Feature Documentation.docx` is superseded by this in-repo file.)
- **Tests** — `ExampleInstrumentedTest.kt` / `ExampleUnitTest.kt` are untouched template stubs. `CsvImportTest.kt` now covers the CSV parser, but nothing yet exercises the DAOs, the Awarded/Owed/fulfillment logic, or the patch-icon spec mapping.
- **In-app help ↔ FEATURES.md coupling** — the in-app Help now renders the bundled `FEATURES.md` directly, so keeping the doc current (already required) also keeps the help current. Note the help renders a *subset* of Markdown (headings/bullets/paragraphs/bold/italic/code); if a section ever needs a table or link, extend the renderer in `ui/components/HelpDialog.kt`.
- **Real Room migrations** — `AppDatabase` still uses `fallbackToDestructiveMigration()` as the catch-all for any version gap other than 9→10 (which now has a real `Migration`, see NOTES.md 2026-07-04). Any future schema bump from version 10 onward should get its own real `Migration` too, now that the app carries session/award history worth preserving.
- ~~**Bulk player import**~~ — done 2026-07-05: CSV upload for both Players and Teams (see log below).
- **Keeping the patch catalog current** — APA occasionally adds or retires patches. `DefaultPatchTypes.SEEDS` (the catalog) and `PatchIcons.ICON_SPECS` (the icon-per-category map) are the two places to update; the catalog re-seeds automatically on next app open, and an unmapped `iconKey` just falls back to a generic icon, so this is a non-breaking, low-risk update path.

## Log

### 2026-07-05

- **iOS port Phase 2 — the four list screens.** Built the SwiftUI counterparts of the Android list screens under `ios/PatchTracker/UI/`: **Patches** (`PatchListView` — events sorted date-desc/division/name, grouped one row per event, the interdependent Session/Division/Date/Player faceted filters + Awarded/Owed segmented status filter, the gold "Repeat" flag computed by `(player, patchType, session, division)` key, per-line **Mark Fulfilled**, swipe-to-delete with confirm, and a photo thumbnail), **Players**, **Teams** (with the division filter), and **Sessions** (list with Current/Finalized pills + a working "start new session" that flips `isCurrent`). Added shared components — `Badges.swift` (`DateBadge`, `InitialsAvatar`, `StatusBadge`, `RepeatBadge`, `TagPill`, `Date.leagueFormatted()`) and `FilterMenu.swift` (the generic faceted dropdown) — and status/badge colors to the theme. Navigation is wired via `NavigationLink`/`.navigationDestination` (Players→detail, Teams→detail, Sessions→detail) and `.sheet` (patch add/edit); the destination/edit screens are `ContentUnavailableView` stubs pending Phase 3. Reads use SwiftData `@Query` directly on the object graph (no flattened projection needed — SwiftUI walks `event.lines`/`event.player`/`event.session`). **Still not compiled** — no Xcode in this Windows environment; unverified against a compiler. Android app unchanged.
- Wrote **`IOS_PORT_PLAN.md`** and **scaffolded a native iOS port** under `ios/`. Decisions (confirmed with the user): native **SwiftUI + SwiftData** rewrite (not KMP/Flutter), **iOS 17** minimum, universal **iPhone + iPad**, iOS-only backups (no Android `.zip` cross-compat requirement). The scaffold is authored on Windows and generated on a Mac via **XcodeGen** (`ios/project.yml`, `.xcodeproj` gitignored; `xcodegen generate` then open). Covers Phase 0 (project config, ZIPFoundation SPM dep, camera usage string, asset catalog) and Phase 1 core: the seven Room entities ported to SwiftData `@Model`s with matching cascade/relationship rules (`ios/PatchTracker/Data/Models/`), the 33-patch default catalog + launch-time self-heal seeder, `PLAYER_NUMBER_LENGTH`/`DIVISION_LENGTH`/`MAX_TEAM_PLAYERS` constants, a `DateOnly` helper (the `LocalDate`→`Date` day-normalization gotcha), `PhotoStorage` storing photos by **relative** filename (iOS sandbox paths move across reinstall — the one place the Android absolute-path approach must not be copied), the league-blue theme, the 25-key patch-icon system remapped to **SF Symbols**, and a five-tab `TabView` shell. Patch Types is the one fully-wired screen (reads seeds via `@Query`, renders `PatchIcon`); the other four tabs are stubs pending Phases 2–6. **Not built on-device** — no Mac/Xcode available in this (Windows) environment, so the Swift is unverified against a compiler. Android app unchanged.
- Cut the first **distributable signed release APK, v0.1.7** and published it as a GitHub Release asset (`patch-tracker-v0.1.7.apk`). Bumped `versionCode` 4→5 / `versionName` "0.1.4"→"0.1.7" to align with FEATURES.md. Added a release `signingConfig` reading a **gitignored** `keystore.properties` (+ `keystore/` holding the `.jks`), with the config guarded so a clone without the secrets still builds (unsigned). Generated the RSA keystore with `keytool`; **it must be backed up — every future update must use the same key.** Verified via `apksigner verify` (V2 OK) and `aapt dump badging` (0.1.7 / code 5); skipped on-device install to avoid wiping the reloaded test data (release key ≠ installed debug key). See NOTES.md 2026-07-05.
- Added **in-app Help on every tab**, sourced from `FEATURES.md` itself so it can never drift from the doc. A Gradle `copyFeaturesDoc` task bundles the repo-root `FEATURES.md` into the app's assets at build time (`sourceSets["main"].assets.srcDir(<generated>)` + `preBuild dependsOn`; confirmed `assets/FEATURES.md` in the APK). New `ui/help/HelpContent.kt` splits the doc into per-H2 sections; new `ui/components/HelpDialog.kt` provides a reusable `HelpAction` top-bar button + a full-screen `HelpDialog` with a ~90-line Markdown-subset renderer (### / bullets with wrapped-line joining / paragraphs / `**bold**` / `*italic*` / `` `code` ``). Each tab maps to its section (Sessions shows "Sessions" + "Data & backups"). No new dependency. Bumped `FEATURES.md` to v0.1.7 (added a note that the guide is available in-app), NOTES.md entry added. Verified on device: Patches, Sessions, and Teams help render headings/bullets/bold/monospace and scroll. See NOTES.md 2026-07-05.
- Added **CSV bulk import for Players and Teams** (upload icon in each tab's top bar → system file picker → summary dialog). New `data/CsvImport.kt` holds a DB-free RFC-4180-style `parseCsv` (quoted fields, escaped `""`, quoted newlines, CRLF, BOM), a flexible header-name→column mapper (`CsvHeader`), and the `ImportSummary` result; `PatchRepository.importPlayersCsv/importTeamsCsv` do validation + inserts. Players need `name` + 5-digit-unique `playerNumber` (optional phone/email); teams use fixed `player1..player8` columns of player numbers (captain = player1). Invalid rows are **skipped and reported** (not all-or-nothing); the in-app rules are mirrored exactly, including **one team per division per player** enforced with a running occupancy map so two teams in one import can't double-book a player, and re-import idempotency (existing name+division skipped). No schema change — just non-observed one-shot list DAO queries. Added parser unit tests ([CsvImportTest.kt](app/src/test/java/com/prolocity/patchtracker/data/CsvImportTest.kt), 6 passing) and starter files in [docs/csv-templates/](docs/csv-templates/). Bumped `FEATURES.md` to v0.1.6, NOTES.md entry added. Verified end-to-end on device from an empty app: players → 4 added / 3 skipped; teams → 3 added / 1 skipped / 2 warnings; Cue Crew roster showed Alex as captain. See NOTES.md 2026-07-05.
- Added a **Division** filter to the Teams list ([TeamListScreen.kt](app/src/main/java/com/prolocity/patchtracker/ui/teams/TeamListScreen.kt)) — a dropdown of the distinct divisions present in the team set (plus "All Divisions"), matching the faceted-dropdown look of the Patches list. Options are sorted; the current selection is unioned back in so it stays clearable. A self-contained `DivisionFilterDropdown` (the Patches-list `FilterDropdown` is private) filters the list client-side, with a "No teams in this division." empty state. Bumped `FEATURES.md` to v0.1.5. Verified on the device: selecting Division 692 narrowed the list from all teams to exactly the 5 Div-692 teams.
- Created `FEATURES.md`, an in-repo end-user/league-rep-facing feature guide (current through v0.1.4: the five tabs, patch awards with Awarded/Owed + fulfillment, filters, Repeat badge, sharing, players/teams validation, patch catalog, and sessions/export/finalize). Added a CLAUDE.md "Project logs" convention to update it as part of every functional change (bump its header, skip pure refactors). Supersedes the old external `.docx` feature doc. Docs-only change.
- Captured the dataset built up on the testing device and committed it under `testdata/` so it can be reloaded on demand instead of re-entered by hand: `test_data.sql` (a data-only SQLite dump — 95 players, 35 teams/219 memberships, 32 patch types, 1 session, 24 award events/27 lines), `load_test_data.sh` (reload onto a connected device), `dump_test_data.sh` (regenerate the SQL from the device), and a `README.md`. Reload pulls the app's current db and applies the data into it (reusing the installed schema so it survives Room version bumps), restores `sqlite_sequence` so later in-app inserts don't collide, and pushes back via a binary-safe `adb push` + `run-as cp` hop. Verified end-to-end on the device (correct row counts, clean FK check, app renders the reloaded session/awards). See NOTES.md 2026-07-05 for the Git-Bash/adb gotchas encoded in the scripts. No app code changes.
- Added a default sort to the Patches list — Date Earned (newest first), then Division, then Player Name ([PatchListScreen.kt](app/src/main/java/com/prolocity/patchtracker/ui/patches/PatchListScreen.kt) group sort). Also made `dump_test_data.sh` emit `patch_award_events` (and their lines) in that same display order so `test_data.sql` reads top-to-bottom like the screen, and regenerated + reloaded the dataset. Verified the on-device list order matches. (Cosmetic for the file; the app sorts at display time regardless of row order.)
- Opened version 0.1.4 (`versionCode` 3→4, `versionName` "0.1.3"→"0.1.4") as the marker for the next round of work — no feature bundled with the bump itself. Committed and tagged `v0.1.4`.
- Enforced the Division field to exactly 3 digits on both the Patch Award and Team edit screens: input is filtered to digits and capped at 3 (max), Save is gated on `division.length == 3` (min), and the field shows an error state + "Must be exactly 3 digits" hint below 3. Added a shared `DIVISION_LENGTH` const in the data package. Verified on the device (typed 5/4 digits → truncated to 3; 2 digits → red error state). All existing data already uses 3-digit codes, so no migration needed.
- Made `playerNumber` a unique identifier for players. Added a unique index on `players.playerNumber` (Room entity + `MIGRATION_11_12`, DB version 11→12) as the integrity backstop, and validation in the Add/Edit Player screen: Save is blocked with an "Another player already has this number" error when the entered number matches another player's (self excluded when editing). Verified on the device — migration ran cleanly (95 players intact, index present), duplicate blocks Save, unique clears it, and editing a player keeps their own number valid.
- Made Player Number a required, exactly-5-digit field (added `PLAYER_NUMBER_LENGTH` const): input filtered to digits and capped at 5 (max), Save gated on `length == 5` (min + required), with a Number keyboard and an error/hint below the field ("Must be exactly 5 digits", or the duplicate message when applicable — duplicate takes priority). All 95 existing numbers are already 5-digit numeric, so nothing was invalidated. Verified on the device (7 digits → truncated to 5; 2 digits → red error + Save disabled; existing 5-digit number → duplicate error).
- Made the Player Name field's required rule explicit/visible: it was already gated in `canSave` (`name.isNotBlank()`), now the field also shows an error state + "Required" hint while blank. Verified on the device (blank name → red "Required", Save disabled even with a valid number; name entered → Save enabled).
- Enforced "one team per division per player" on the Team edit screen: the player-slot dropdowns now exclude anyone already rostered on another team in this team's division (computed from the `teams` flow, keyed on the current `division` field, and excluding this team itself so its own members stay selectable). Verified on the device — editing Corner Pocket (Div 066), Jamie Porter and Marco Fields (on other Div 066 teams) were absent from the dropdown while non-066 players like Jamie Cole/Reed and Reggie Marsh remained.
- Replaced the Patch Award screen's Player picker (a full dropdown of all players) with a type-to-search lookup: the user types a name (or number) and, once ≥ `PLAYER_SEARCH_MIN_CHARS` (2) characters are entered, matching players appear as suggestions in an editable `ExposedDropdownMenuBox`. Picking one sets the selection; editing the text again clears it until another is picked (so `selectedPlayer` is only ever an explicit choice), and a clear (X) button resets it. Pre-fills the chosen player when editing an existing award. Verified on the device (typed "wil" → filtered list; picked Will Barnes → field filled; opened an existing award → Quinn Fisher pre-filled).
- Extracted that lookup into a shared `ui/components/PlayerLookupField` (takes an already-narrowed candidate list + label) and reused it for the Team edit screen's 8 player slots, replacing the old `PlayerSlotDropdown`. The existing narrowing rules are preserved by passing each slot's `availablePlayers` (already filtered for "not in another slot of this team" and "not on another team in this division") as the candidate list, so the typeahead only narrows further by the typed text. Verified on the device — in Corner Pocket (Div 066), typing "jamie" showed Jamie Cole/Reed but not Jamie Porter (on another 066 team), and "sasha" showed "No players match" (already in slot 1); existing members pre-filled.
- Added three combinable filters to the Patches list — Division, Player, and Date Earned dropdowns (on top of the existing Session dropdown and Awarded/Owed chips). Options are drawn from the awards in the selected session (distinct divisions, players, and dates), and all filters AND together via a generic `FilterDropdown` composable; a null selection = "All". The three reset when the session changes (their options are session-scoped). Verified on the device: Division 694 narrowed to 694 awards, adding Player Jamie Reed narrowed to his single 694 award, and adding Date Jun 25 (a non-match) produced "No patches match this filter" — confirming the three combine.
- Made those filter dropdowns interdependent (faceted): each dropdown now offers only values still present in the records that match the OTHER active filters (its own excluded) plus the session/status filters, so selecting one narrows the others' options. Refactored the group-level checks into shared `matchesSession/Division/Player/Date/Status` predicates used by both the visible list and the per-facet option lists; the active selection is unioned back into its own options so it stays visible/clearable even when the combination matches nothing. Verified on the device: with Division 694 the Player list dropped to only players with a 694 award (682/684 players removed), and adding Jamie Reed narrowed the Date list to just Jul 2, 2026.
- Changed the Division field on the Patch Award edit screen from a free-text field to a `DivisionDropdown` whose options are the divisions of the teams the selected player is rostered on (from the `teams` flow). One team → one option (auto-selected on new awards); multiple teams → all their divisions offered. Switching player reconciles the division (clears it if not valid for the new player, or auto-picks their single division); existing awards keep their recorded division (auto-management guarded to new awards, and the recorded value is unioned into the options so it's never dropped). Placeholder guides the empty states ("Select a player first" / "Player is not on a team"). Verified on the device: Avery Shah (1 team, 682) auto-filled 682; Robin Cortez (684 & 694) offered both with no auto-select and picking 694 set it.
- Added a "No division" option to the Patch Award Division dropdown so an award can be kept for a player who isn't on a team (e.g. removed from a lineup). Represented as an empty `division` string — no schema change; the edit screen's `division` state became `String?` (null = no choice yet, "" = No division, else a code), Save now requires a choice was made (`division != null`) rather than a 3-digit value, and "No division" always appears in the dropdown regardless of the player's teams. Displays as "No division" in the Patches list, the Session review, and the Division filter (which now offers "No division" as a filter value when such awards exist). Verified on the device end-to-end: created a no-division award for Robin Cortez, saw it save (blank division) and render as "No division", then deleted it to restore the dataset. See NOTES.md 2026-07-05.
- Added a gold "Repeat" indicator on patch lines that aren't the first time a player earned that patch type within the same session **and** division (a duplicate award). Computed client-side (no schema change) across all award lines: key is `(playerId, patchTypeId, sessionId, division)`, lines sorted by `(dateEarned, eventId, lineId)`, every line after the earliest flagged — so same-patch/different-division stays two first awards, and repeats spanning separate award entries (or a second identical line in one event) are caught. New `RepeatBadge` in `Common.kt` (`LeagueGold`, distinct from the green/amber status badge); also added to `SessionReviewScreen` (keyed by `(playerNumber, division, patchName)` since the backup has no ids). Verified on the device: Leo Grant's 9-Ball on the Break (div 692) — earliest event unflagged, later event's two identical lines both flagged Repeat, his 8-Ball (div 684) unflagged. See NOTES.md 2026-07-05.

### 2026-07-04

- Built the initial app end-to-end: Room + Compose + Navigation, Players/Patches/Patch Types screens, Awarded/Owed status with fulfillment tracking. Restyled to an APA Scorekeeper-inspired look (league-blue theme, flat list rows, date-chip badges). Committed and tagged `v0.1`.
- Replaced the placeholder patch catalog with the real 31-patch APA list, added an original per-category icon system, and added camera capture for custom patch photos. Fixed a badge-overflow rendering bug and a seed-catalog-lost-on-migration bug along the way. Committed.
- Added `CLAUDE.md` (build commands, architecture, working conventions) and this NOTES.md/PROGRESS.md pair, replacing stray files from an unrelated project that had been sitting in the repo root. Committed.
- Split the single "Clean Sweep" patch type into "8-Ball Clean Sweep" and "9-Ball Clean Sweep" (per-game variants, matching the pattern of Mini Slam/Break and Run), bumped `AppDatabase` to version 4, and verified both appear correctly on a physical device. Committed.
- Replaced the default Android Studio green-arrow app icon with the "What Comes Next?" logo (adaptive icon: white background + inset logo bitmap as foreground), verified on a physical device. Committed.
- Added a `division` field to patch awards (the APA division the player was in when the patch was earned, distinct from the player's own current division — leagues can move players between divisions mid-season). Bumped `AppDatabase` to version 5. Verified add/edit/display/delete end-to-end on a physical device. Committed.
- Reordered the Add/Edit Patch form fields to Player, Session, Division, Date Earned, Patch (Status stays last). Committed.
- Added a fourth "Teams" tab: teams have a name, a division, and up to 8 players drawn from the existing roster (many-to-many via a `team_members` join table, `MAX_TEAM_PLAYERS = 8` enforced in the repository and the edit-screen checklist). Bumped `AppDatabase` to version 6. Verified add/edit/reassign-members/delete end-to-end on a physical device. Committed.
- Reworked the Team screen into view/edit modes (opening an existing team shows a read-only summary + "Edit Team" button, rather than dropping straight into editable fields) and replaced the player checklist with 8 explicit numbered player-slot dropdowns, slot 1 being the team captain. Added `position` to `team_members` to preserve slot order. Bumped `AppDatabase` to version 7. Verified view/edit toggle, captain display, and duplicate-selection prevention across slots on a physical device. Committed.
- Reworked the Player screen to match: opening an existing player now shows a read-only summary with an "Edit Player" button instead of immediately-editable fields. Added optional `phoneNumber`/`email` contact fields. The view mode also lists the player's earned patches (grouped by patch type with a "×N" count, reusing `PatchIcon`) and the teams they're a member of, both computed by filtering the existing patch-award/team flows rather than new DAO queries. Bumped `AppDatabase` to version 8. Verified add/view/edit round-trip, patch-count aggregation (×2 for a repeated patch type), team-membership display, and cascade-delete on a physical device. Committed.
- Restructured patch awards from one-patch-per-record to one award entry (player, session, division, date, optional photo) holding multiple patches, each with its own independent Awarded/Owed status. Split the old `PatchAward` entity into `PatchAwardEvent` + `PatchAwardLine`; the edit screen now supports "+ Add Another Patch"/per-line delete, and the list screen groups lines by event while still filtering/marking-fulfilled per individual patch. Added camera capture (reusing the existing `PatchPhotos.kt` plumbing) for a photo of the player with their patches. Bumped `AppDatabase` to version 9. Verified on a physical device: multi-patch entry with mixed statuses, photo capture/retake, status-filter chips narrowing to matching lines within a group, per-line Mark Fulfilled, edit-screen reload of an existing multi-line event (including the photo and fulfilled state), and event delete. Committed.
- Added a `Session` entity replacing the free-text session field on patch awards: an app-wide "current session" that new awards default to, a new Sessions tab (start/rename/set-current/delete), per-session "Clear Patches for This Session," export to a self-contained `.zip` backup (JSON + photos) via the system file picker, and read-only review of a reopened backup. Wrote the project's first real Room `Migration` (9→10) to preserve existing free-text session values as real `Session` rows rather than wiping via `fallbackToDestructiveMigration()`. Verified the migration on a physical device by upgrading over an existing install with two distinct sessions logged, then drove the full flow end-to-end: session-filtered Patches list, new-session creation becoming current, export/clear/review round-trip, and the "can't delete the current session" guard. Committed.
- Added session finalization: exporting a session now marks it finalized (`MIGRATION_10_11`), locking its patch awards from further add/edit (disabled Save, hidden delete, a locked banner in the edit screen, hidden per-row mutation buttons in the list) and gating deletion on being finalized — the current session still can never be deleted regardless. Verified on a physical device: export-then-lock on an existing award, a finalized session excluded from the session picker for new awards, and delete blocked-then-allowed as a session moved from current → finalized → non-current. Committed.
- Opened version 0.1.3 (`versionCode` 2→3, `versionName` "0.1.2"→"0.1.3") as the marker for the next round of work — no feature bundled with the bump itself. Committed and tagged `v0.1.3`.
- Replaced the app icon with a new pool-ball design (green felt, 8/9 balls) provided as a 512px full-bleed PNG. Since the art centers the balls with felt margin, used it full-bleed as the adaptive-icon foreground (dropped the old 18% inset wrapper `drawable/ic_launcher_foreground.xml`) so the launcher mask trims the outer felt while the balls stay in the safe zone; changed `ic_launcher_background` from white to felt green (`#2E7A5A`) for launcher parallax edge-reveal. Verified on a physical device (App info + taskbar render the masked icon correctly). Committed.
- Added "share selected patch awards to Facebook." Researched that Meta removed the Groups API entirely (Apr 2024) and that Facebook strips pre-filled captions from image shares — so this is a system-share-sheet handoff (user picks the group and posts manually), packaged as the raw award photos + a name/patches summary copied to the clipboard for the caption (no image generation, no Graph API/SDK). New selection mode on the Patches list (top-bar checklist icon / long-press, checkboxes, `combinedClickable`, suppressed row actions + FAB, `BackHandler`) plus a `SharePatchAwards.kt` helper reusing the existing `patchPhotoUriFor` FileProvider mapping. No data-layer/ViewModel/manifest/dependency changes. Verified end-to-end on a physical device (photo multi-share, clipboard summary, text-only fallback). Committed.
