# PROGRESS.md — Patch Tracker

## Current state

Functional MVP, verified end-to-end on a physical device. Tracks a player roster (name, number, optional phone/email, with a detail view showing earned-patch counts and team memberships), teams (name, division, up to 8 players each), a 33-patch real-APA catalog (with a per-category icon system plus camera-captured photos for custom patches), and patch award entries — one player/session/division/date per entry, holding multiple patches each with its own independent Awarded/Owed status and fulfillment tracking, plus an optional photo of the player with the patches. Patch awards are now organized around a real `Session` entity with an app-wide "current session" that new awards default to (a Sessions tab manages starting new sessions, renaming, and switching current); a session's awards can be bulk-cleared once finished, exported to a self-contained `.zip` backup via the system file picker, and a backup can be reopened for read-only review without touching the live database. Exporting a session also finalizes it — locking its awards from further add/edit and being the only way to unlock deletion (the current session can never be deleted, finalized or not). The Patches list also has a selection mode (top-bar checklist icon or long-press) for picking several award records and sharing them out via the system share sheet — the award photos ride along as images and a player-name + patches summary is copied to the clipboard for pasting as the caption (Facebook killed group-posting via API, so posting to the league group is a manual final tap). All work through 2026-07-04 is committed — see `NOTES.md` for the decision/bug log behind it.

**No open blockers.**

## Next action

None pending. Pick from "Suggested next steps" below by priority, or continue with new feature requests as they come in.

## Suggested next steps

- **Tests** — `ExampleInstrumentedTest.kt` / `ExampleUnitTest.kt` are untouched template stubs. Nothing exercises the DAOs, the Awarded/Owed/fulfillment logic, or the patch-icon spec mapping.
- **Real Room migrations** — `AppDatabase` still uses `fallbackToDestructiveMigration()` as the catch-all for any version gap other than 9→10 (which now has a real `Migration`, see NOTES.md 2026-07-04). Any future schema bump from version 10 onward should get its own real `Migration` too, now that the app carries session/award history worth preserving.
- **Bulk player import** — every player is currently added one at a time; a CSV roster import would help onboarding a full league.
- **Keeping the patch catalog current** — APA occasionally adds or retires patches. `DefaultPatchTypes.SEEDS` (the catalog) and `PatchIcons.ICON_SPECS` (the icon-per-category map) are the two places to update; the catalog re-seeds automatically on next app open, and an unmapped `iconKey` just falls back to a generic icon, so this is a non-breaking, low-risk update path.

## Log

### 2026-07-05

- Captured the dataset built up on the testing device and committed it under `testdata/` so it can be reloaded on demand instead of re-entered by hand: `test_data.sql` (a data-only SQLite dump — 95 players, 35 teams/219 memberships, 32 patch types, 1 session, 24 award events/27 lines), `load_test_data.sh` (reload onto a connected device), `dump_test_data.sh` (regenerate the SQL from the device), and a `README.md`. Reload pulls the app's current db and applies the data into it (reusing the installed schema so it survives Room version bumps), restores `sqlite_sequence` so later in-app inserts don't collide, and pushes back via a binary-safe `adb push` + `run-as cp` hop. Verified end-to-end on the device (correct row counts, clean FK check, app renders the reloaded session/awards). See NOTES.md 2026-07-05 for the Git-Bash/adb gotchas encoded in the scripts. No app code changes.
- Added a default sort to the Patches list — Date Earned (newest first), then Division, then Player Name ([PatchListScreen.kt](app/src/main/java/com/prolocity/patchtracker/ui/patches/PatchListScreen.kt) group sort). Also made `dump_test_data.sh` emit `patch_award_events` (and their lines) in that same display order so `test_data.sql` reads top-to-bottom like the screen, and regenerated + reloaded the dataset. Verified the on-device list order matches. (Cosmetic for the file; the app sorts at display time regardless of row order.)
- Opened version 0.1.4 (`versionCode` 3→4, `versionName` "0.1.3"→"0.1.4") as the marker for the next round of work — no feature bundled with the bump itself. Committed and tagged `v0.1.4`.
- Enforced the Division field to exactly 3 digits on both the Patch Award and Team edit screens: input is filtered to digits and capped at 3 (max), Save is gated on `division.length == 3` (min), and the field shows an error state + "Must be exactly 3 digits" hint below 3. Added a shared `DIVISION_LENGTH` const in the data package. Verified on the device (typed 5/4 digits → truncated to 3; 2 digits → red error state). All existing data already uses 3-digit codes, so no migration needed.
- Made `playerNumber` a unique identifier for players. Added a unique index on `players.playerNumber` (Room entity + `MIGRATION_11_12`, DB version 11→12) as the integrity backstop, and validation in the Add/Edit Player screen: Save is blocked with an "Another player already has this number" error when the entered number matches another player's (self excluded when editing). Verified on the device — migration ran cleanly (95 players intact, index present), duplicate blocks Save, unique clears it, and editing a player keeps their own number valid.
- Made Player Number a required, exactly-5-digit field (added `PLAYER_NUMBER_LENGTH` const): input filtered to digits and capped at 5 (max), Save gated on `length == 5` (min + required), with a Number keyboard and an error/hint below the field ("Must be exactly 5 digits", or the duplicate message when applicable — duplicate takes priority). All 95 existing numbers are already 5-digit numeric, so nothing was invalidated. Verified on the device (7 digits → truncated to 5; 2 digits → red error + Save disabled; existing 5-digit number → duplicate error).
- Made the Player Name field's required rule explicit/visible: it was already gated in `canSave` (`name.isNotBlank()`), now the field also shows an error state + "Required" hint while blank. Verified on the device (blank name → red "Required", Save disabled even with a valid number; name entered → Save enabled).
- Enforced "one team per division per player" on the Team edit screen: the player-slot dropdowns now exclude anyone already rostered on another team in this team's division (computed from the `teams` flow, keyed on the current `division` field, and excluding this team itself so its own members stay selectable). Verified on the device — editing Circlin' Around (Div 066), Chris Gonzalez and David Evans (on other Div 066 teams) were absent from the dropdown while non-066 players like Chris Hodge/Williams and Damian Aviles remained.

### 2026-07-04

- Built the initial app end-to-end: Room + Compose + Navigation, Players/Patches/Patch Types screens, Awarded/Owed status with fulfillment tracking. Restyled to an APA Scorekeeper-inspired look (league-blue theme, flat list rows, date-chip badges). Committed and tagged `v0.1`.
- Replaced the placeholder patch catalog with the real 31-patch APA list, added an original per-category icon system, and added camera capture for custom patch photos. Fixed a badge-overflow rendering bug and a seed-catalog-lost-on-migration bug along the way. Committed.
- Added `CLAUDE.md` (build commands, architecture, working conventions) and this NOTES.md/PROGRESS.md pair, replacing stray files from an unrelated project that had been sitting in the repo root. Committed.
- Split the single "Clean Sweep" patch type into "8-Ball Clean Sweep" and "9-Ball Clean Sweep" (per-game variants, matching the pattern of Mini Slam/Break and Run), bumped `AppDatabase` to version 4, and verified both appear correctly on a physical device. Committed.
- Replaced the default Android Studio green-arrow app icon with the "What Comes Next?" logo (adaptive icon: white background + inset logo bitmap as foreground), verified on a physical device. Committed.
- Added a `division` field to patch awards (the APA division the player was in when the patch was earned, distinct from the player's own current division — leagues can move players between divisions mid-season). Bumped `AppDatabase` to version 5. Verified add/edit/display/delete end-to-end on a physical device. Committed.
- Reordered the Add/Edit Patch form fields to Player, Session, Division, Date Earned, Patch (Status stays last). Committed.
- Added a fourth "Teams" tab: teams have a name, a division, and up to 8 players drawn from the existing roster (many-to-many via a `team_members` join table, `MAX_TEAM_PLAYERS = 8` enforced in the repository and the edit-screen checklist). Bumped `AppDatabase` to version 6. Verified add/edit/reassign-members/delete end-to-end on a physical device. Committed.
- Reworked the Team screen into view/edit modes (opening an existing team shows a read-only summary + "Edit Team" button, rather than dropping straight into editable fields) and replaced the player checklist with 8 explicit numbered player-slot dropdowns, slot 1 being the team captain. Added `position` to `team_members` to preserve slot order. Bumped `AppDatabase` to version 7. Verified view/edit toggle, captain display, and duplicate-selection prevention across slots on a physical device. Committed.
- Reworked the Player screen to match: opening an existing player now shows a read-only summary with an "Edit Player" button instead of immediately-editable fields. Added optional `phoneNumber`/`email` contact fields. The view mode also lists the player's earned patches (grouped by patch type with a "×N" count, reusing `PatchIcon`) and the teams they're a member of, both computed by filtering the existing patch-award/team flows rather than new DAO queries. Bumped `AppDatabase` to version 8. Verified add/view/edit round-trip, patch-count aggregation (×2 for a repeated patch type), team-membership display, and cascade-delete on a physical device. Committed.
- Restructured patch awards from one-patch-per-record to one award entry (player, session, division, date, optional photo) holding multiple patches, each with its own independent Awarded/Owed status. Split the old `PatchAward` entity into `PatchAwardEvent` + `PatchAwardLine`; the edit screen now supports "+ Add Another Patch"/per-line delete, and the list screen groups lines by event while still filtering/marking-fulfilled per individual patch. Added camera capture (reusing the existing `PatchPhotos.kt` plumbing) for a photo of the player with their patches. Bumped `AppDatabase` to version 9. Verified on a physical device: multi-patch entry with mixed statuses, photo capture/retake, status-filter chips narrowing to matching lines within a group, per-line Mark Fulfilled, edit-screen reload of an existing multi-line event (including the photo and fulfilled state), and event delete. Committed.
- Added a `Session` entity replacing the free-text session field on patch awards: an app-wide "current session" that new awards default to, a new Sessions tab (start/rename/set-current/delete), per-session "Clear Patches for This Session," export to a self-contained `.zip` backup (JSON + photos) via the system file picker, and read-only review of a reopened backup. Wrote the project's first real Room `Migration` (9→10) to preserve existing free-text session values as real `Session` rows rather than wiping via `fallbackToDestructiveMigration()`. Verified the migration on a physical device by upgrading over an existing install with two distinct sessions logged, then drove the full flow end-to-end: session-filtered Patches list, new-session creation becoming current, export/clear/review round-trip, and the "can't delete the current session" guard. Committed.
- Added session finalization: exporting a session now marks it finalized (`MIGRATION_10_11`), locking its patch awards from further add/edit (disabled Save, hidden delete, a locked banner in the edit screen, hidden per-row mutation buttons in the list) and gating deletion on being finalized — the current session still can never be deleted regardless. Verified on a physical device: export-then-lock on an existing award, a finalized session excluded from the session picker for new awards, and delete blocked-then-allowed as a session moved from current → finalized → non-current. Committed.
- Opened version 0.1.3 (`versionCode` 2→3, `versionName` "0.1.2"→"0.1.3") as the marker for the next round of work — no feature bundled with the bump itself. Committed and tagged `v0.1.3`.
- Replaced the app icon with a new pool-ball design (green felt, 8/9 balls) provided as a 512px full-bleed PNG. Since the art centers the balls with felt margin, used it full-bleed as the adaptive-icon foreground (dropped the old 18% inset wrapper `drawable/ic_launcher_foreground.xml`) so the launcher mask trims the outer felt while the balls stay in the safe zone; changed `ic_launcher_background` from white to felt green (`#2E7A5A`) for launcher parallax edge-reveal. Verified on a physical device (App info + taskbar render the masked icon correctly). Committed.
- Added "share selected patch awards to Facebook." Researched that Meta removed the Groups API entirely (Apr 2024) and that Facebook strips pre-filled captions from image shares — so this is a system-share-sheet handoff (user picks the group and posts manually), packaged as the raw award photos + a name/patches summary copied to the clipboard for the caption (no image generation, no Graph API/SDK). New selection mode on the Patches list (top-bar checklist icon / long-press, checkboxes, `combinedClickable`, suppressed row actions + FAB, `BackHandler`) plus a `SharePatchAwards.kt` helper reusing the existing `patchPhotoUriFor` FileProvider mapping. No data-layer/ViewModel/manifest/dependency changes. Verified end-to-end on a physical device (photo multi-share, clipboard summary, text-only fallback). Committed.
